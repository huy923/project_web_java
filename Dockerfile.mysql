FROM mysql:8.0

# Copy init script into the image
COPY mysql-init/01-init.sql /docker-entrypoint-initdb.d/01-init.sql

# Ensure proper permissions
RUN chmod 644 /docker-entrypoint-initdb.d/01-init.sql